#include <stdio.h>



#define BUF_SIZE 1024
char buf[BUF_SIZE];

int main(void) {
	/*
	char in_c;
	int idx = 0;
	
	while(in_c != '\n' && idx < BUF_SIZE) {
		in_c = getchar();
		buf[idx] = in_c;
		idx++;
	}
	*/
	printf("%x\n", EOF);	
}
